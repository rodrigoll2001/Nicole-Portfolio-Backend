services:
  - type: web
    name: nicole-portfolio
    env: java
    plan: free
    buildCommand: ./mvnw clean package -DskipTests
    startCommand: java -jar target/nicole-portfolio-0.0.1-SNAPSHOT.jar
    envVars:
      - key: PORT
        value: 10000
